/**
 * Enum representing the different roles in the system.
 * These roles define the type of user and their associated permissions and functionalities.
 */

package entity;

public enum Role {
    APPLICANT,
    HDB_OFFICER, 
    HDB_MANAGER
}